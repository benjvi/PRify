package git

/*func resolveRemote() error {
	// if remote is explicitly specified in pushconfig, use that

	// else if git repo has a default remote, use that

	// else return nil to show we don't have a remote

	return nil
}

type remote struct {
	name string
	url string
	remoteType string
}*/

/*func fetchRemoteBranch() {
	// fetch branch or fail
	remoteBranchExists := false
	if remote != nil {
		remoteBranchExists, err := fetchBranch(b.Name, remote)
		if err != nil {
			return err
		}


	}
}*/